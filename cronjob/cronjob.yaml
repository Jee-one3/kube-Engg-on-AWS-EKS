apiVersion: batch/v1
kind: CronJob
metadata:
  name: test-cron
  namespace: capstone-ns1
spec:
  schedule: "* * * * *"
  jobTemplate:
    spec:
      template:
        spec:
          containers:
          - name: busybox
            image: busybox
            command: ["echo", "Hello test!!!"]
          restartPolicy: Never
